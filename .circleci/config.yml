version: 2
jobs:
  build:
    docker:
      - image: circleci/python
    steps:
      - checkout
      - run: pip install -U pytest
      - run: pytest
  mypy:
    docker:
      - image: circleci/python
    steps:
      - checkout
      - run: pip3 install mypy
      - run: mypy -p capstone360
