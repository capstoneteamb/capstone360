version: 2
jobs:
  build:
    docker:
      - image: circleci/python:3.5-stretch
    steps:
      - run: sudo apt-get install build-essential debhelper devscripts equivs dh-virtualenv
      - run: sudo apt-get install libpython3.5
      - checkout
      - run: dpkg-buildpackage -us -uc
  test:
    docker:
      - image: circleci/python
    steps:
      - checkout
      - run: sudo pip install -U pytest
      - run: sudo pip install -r requirements.txt
      - run: python3 -m pytest tests/
  mypy:
    docker:
      - image: circleci/python
    steps:
      - checkout
      - run: sudo pip3 install -U mypy
      - run: mypy -p capstone360
  lint:
    docker:
      - image: circleci/python
    steps:
      - checkout
      - run: sudo pip3 install -U flake8
      - run: flake8 --max-line-length=110

workflows:
  version: 2
  build_and_test:
    jobs:
      - test
      - lint
      - build
      # - mypy
